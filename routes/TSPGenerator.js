/**
 * TSP Generator.
 *
 * @param type
 *  Generator type.
 * @param query
 * @constructor
 */
function TSPGenerator(type, query) {
    this.type = type;
    this.query = query;
}

/**
 * Generates modes.
 */
TSPGenerator.prototype.generate = function() {

};

/**
 * Returns model data.
 */
TSPGenerator.prototype.toArray = function() {

};

module.exports = TSPGenerator;
